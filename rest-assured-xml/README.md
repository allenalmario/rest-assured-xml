This repo takes the restAssured framework and uses it to work with soap api requests which are in XML format rather than json.

works with calculator wsdl: http://www.dneonline.com/calculator.asmx?WSDL